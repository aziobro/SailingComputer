# PlatformIO build artifacts
.pio/

# IDF managed components — auto-downloaded via idf_component.yml + dependencies.lock
managed_components/

# macOS
.DS_Store

# Claude session files
.claude/

# Sensitive config (NVS is on-device, but just in case)
*.env
